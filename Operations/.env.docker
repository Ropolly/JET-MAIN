# Docker Development Environment Variables
# This file is used for Docker Compose development setup

# Django Configuration
SECRET_KEY=django-insecure-docker-dev-key
DEBUG=True
DJANGO_SETTINGS_MODULE=backend.settings
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend,frontend

# Database Configuration (PostgreSQL in Docker)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=jeticu_dev
DB_USER=postgres
DB_PASSWORD=dockerpass
DB_HOST=postgres  # Docker service name
DB_PORT=5432

# Legacy PostgreSQL environment variables (for compatibility)
POSTGRES_DB=jeticu_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dockerpass
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis Configuration
REDIS_HOST=redis  # Docker service name
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# HIPAA Encryption (Development key - DO NOT USE IN PRODUCTION)
ENCRYPTION_KEY=ZGV2ZWxvcG1lbnQta2V5LWZvci10ZXN0aW5nLW9ubHk=
ENCRYPTION_KEY_ID=dev-key

# Frontend Configuration
VITE_APP_API_URL=http://localhost:8001/api
VITE_APP_NAME=JET ICU Operations (Docker Dev)

# Development Superuser
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@jeticu.local
DJANGO_SUPERUSER_PASSWORD=admin123

# Development Flags
LOAD_SAMPLE_DATA=false
RUN_SETUP_COMMANDS=true

# DocuSeal API (Development)
DOCUSEAL_API_KEY=iMDk8MquWy9jnnMudnde52Efzfe8HKjx9BdCL4BzePT
DOCUSEAL_BASE_URL=https://api.docuseal.com
DOCUSEAL_JET_ICU_SIGNER_EMAIL=contracts@jeticu.local

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8001